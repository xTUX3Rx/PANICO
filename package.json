{
  "name": "boton-panico",
  "version": "1.0.0",
  "description": "Aplicación Botón de Pánico con login, sesión y logout usando Netlify Functions",
  "main": "backend/server.js",
  "scripts": {
    "start": "netlify dev",        // para correr en local con Netlify
    "build": "echo 'No build step'", 
    "deploy": "netlify deploy"     // si usas la CLI de Netlify
  },
  "author": "Tu Nombre",
  "license": "MIT",
  "dependencies": {
    "netlify-cli": "^17.0.0"       // para ejecutar `netlify dev` y desplegar
  }
}
